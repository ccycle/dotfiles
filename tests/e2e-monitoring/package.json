{
  "name": "dotfiles-e2e-monitoring",
  "version": "1.0.0",
  "private": true,
  "description": "API-driven E2E tests for the monitoring stack (Prometheus/Grafana/Loki), plus a lightweight Grafana browser check, run against an isolated per-worktree stack",
  "type": "module",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1"
  }
}
