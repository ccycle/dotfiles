# User skills

Repo-agnostic Agent Skills deployed globally to `~/.claude/skills` and `~/.agents/skills`. Placement criteria, and the split with the repo-scoped skills at `.agents/skills/`, are documented there.
